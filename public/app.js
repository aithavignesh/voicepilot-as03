let conversationId=null, speechEnabled=true, recognizing=false;
const $=s=>document.querySelector(s);
const api=async(u,o)=>{const r=await fetch(u,o);if(!r.ok)throw new Error(await r.text()||`HTTP ${r.status}`);return r.json()};
function addMsg(role,text,meta=''){const box=$('#messages');const empty=box.querySelector('.welcome');if(empty)empty.remove();const el=document.createElement('div');el.className=`msg ${role}`;const label=document.createElement('small');label.textContent=role==='user'?'YOU':'VOICEPILOT';const body=document.createElement('div');body.textContent=text;el.append(label,body);if(meta){const m=document.createElement('small');m.className='meta';m.textContent=meta;el.append(m)}box.appendChild(el);box.scrollTop=box.scrollHeight}
function setBusy(b){$('#mic').disabled=b||recognizing;$('#sendBtn').disabled=b;document.querySelectorAll('[data-prompt]').forEach(x=>x.disabled=b);$('#input').disabled=b;}
function speak(text){if(!speechEnabled||!('speechSynthesis'in window))return;speechSynthesis.cancel();const u=new SpeechSynthesisUtterance(text);u.rate=.98;u.pitch=1;speechSynthesis.speak(u)}
async function load(){try{const [health,p,h,a,f,ins]=await Promise.all([api('/api/health'),api('/api/project'),api('/api/project/health'),api('/api/project/activity?limit=8'),api('/api/project/files'),api('/api/project/insights')]);$('#connection').textContent=health.database?'LIVE DATABASE':'LIVE DEMO';$('#connection').className='status ok';$('#statMode').textContent=health.database?'POSTGRES':'DEMO';$('#statModeDetail').textContent=health.ai?'AI grounded':'deterministic fallback';$('#projectName').textContent=p.name;$('#projectDesc').textContent=p.description;$('#repo').href=`https://github.com/${p.repository}`;$('#score').textContent=h.score;$('#statHealth').textContent=h.score;$('#healthState').textContent=h.score>=90?'HEALTHY':h.score>=75?'WATCH':'NEEDS ATTENTION';$('#checks').innerHTML=h.checks.map(x=>`<div class="check ${x.status==='warning'?'warn':''}"><b>${x.name}</b><span>${x.score}% · ${x.status}</span></div>`).join('');$('#stack').innerHTML=p.stack.map(x=>`<span class="tag">${x}</span>`).join('');$('#activity').innerHTML=a.map(x=>`<div class="activity-item"><b>${x.title}</b><p>${x.description}</p><small>${new Date(x.createdAt).toLocaleString()} · ${x.actor}</small></div>`).join('');$('#files').innerHTML=f.map(x=>`<div class="file"><div><b>${x.path}</b><small>${x.summary}</small></div><span class="risk ${x.risk}">${x.risk}</span></div>`).join('');$('#riskCount').textContent=`${ins.risk.length} flagged`;$('#risks').innerHTML=ins.risk.map(x=>`<div class="risk-row"><div><b>${x.path}</b><small>${x.reason}</small></div><span class="risk ${x.risk}">${x.risk}</span></div>`).join('')||'<p class="muted">No elevated risks detected.</p>';$('#priorities').innerHTML=ins.priorities.map(x=>`<div class="priority"><b>${x.rank}. ${x.title}</b><p>${x.detail}</p></div>`).join('');$('#capabilityTags').innerHTML=ins.capabilities.map(x=>`<span>${x}</span>`).join('');try{if(!conversationId)conversationId=(await api('/api/conversations',{method:'POST'})).conversationId}catch(e){console.warn('Conversation setup unavailable; direct command mode active',e)}}catch(e){$('#connection').textContent='OFFLINE';$('#connection').className='status';console.error(e)}}
async function send(text){text=(text||'').trim();if(!text)return;addMsg('user',text);$('#input').value='';$('#intent').textContent='THINKING';setBusy(true);try{let d;if(conversationId){try{d=await api(`/api/conversations/${conversationId}/messages`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({text})})}catch(e){d=await api('/api/voice/command',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({text})})}}else d=await api('/api/voice/command',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({text})});const answer=d.answer||d.message||'Command completed.';addMsg('assistant',answer,`${(d.intent||'command').replaceAll('_',' ')} · ${d.source||'action'}`);$('#intent').textContent=(d.intent||'command').replaceAll('_',' ').toUpperCase();speak(answer);if(d.action==='health_scan')await load();}catch(e){console.error(e);addMsg('assistant','I could not process that command. Check your connection and try again.','ERROR');$('#intent').textContent='ERROR';}finally{setBusy(false)}}
$('#form').onsubmit=e=>{e.preventDefault();send($('#input').value)};
document.querySelectorAll('[data-prompt]').forEach(b=>{b.type='button';b.onclick=()=>send(b.dataset.prompt)});
$('#scan').onclick=async()=>{setBusy(true);try{const d=await api('/api/project/scan',{method:'POST'});addMsg('assistant',`Health scan completed. Score: ${d.score}/100.`,'health scan');await load();}catch(e){addMsg('assistant','The health scan could not be completed.','ERROR')}finally{setBusy(false)}};
$('#voiceToggle').onclick=()=>{speechEnabled=!speechEnabled;$('#voiceToggle').textContent=speechEnabled?'🔊':'🔇';$('#voiceToggle').title=speechEnabled?'Spoken answers enabled':'Spoken answers disabled';if(!speechEnabled&&'speechSynthesis'in window)speechSynthesis.cancel()};
const SpeechRecognition=window.SpeechRecognition||window.webkitSpeechRecognition;let rec=null;
if(SpeechRecognition){rec=new SpeechRecognition();rec.lang='en-IN';rec.interimResults=true;rec.continuous=false;rec.onstart=()=>{recognizing=true;$('#live').hidden=false;$('#liveText').textContent='Listening…';$('#mic strong').textContent='Listening…';$('#mic').disabled=true};rec.onresult=e=>{let t='';for(const r of e.results)t+=r[0].transcript;$('#liveText').textContent=t;$('#input').value=t};rec.onend=()=>{const t=$('#input').value.trim();recognizing=false;$('#live').hidden=true;$('#mic strong').textContent='Hold to speak';$('#mic').disabled=false;if(t)send(t)};rec.onerror=e=>{recognizing=false;$('#live').hidden=true;$('#mic strong').textContent='Hold to speak';$('#mic').disabled=false;addMsg('assistant',`Voice input error: ${e.error}. You can type the command instead.`,'VOICE ERROR')}}else{$('#mic strong').textContent='Type instead';$('#mic').title='Speech recognition is not supported in this browser. Use the text box.'}
$('#mic').onclick=()=>{if(!rec||recognizing||$('#mic').disabled)return;try{rec.start()}catch(e){console.warn(e)}};
window.onkeydown=e=>{if(e.code==='Space'&&document.activeElement!==$('#input')&&!$('#mic').disabled&&!recognizing){e.preventDefault();if(rec)try{rec.start()}catch(err){console.warn(err)}}};
load();